{
  "name": "@mcp-toolkit/cors",
  "version": "0.1.0",
  "description": "Origin validation middleware for MCP servers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "test -n \"$(find dist -name \"*.test.js\" 2>/dev/null)\" && node --test dist/**/*.test.js || echo \"No tests found, skipping\""
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cors",
    "origin",
    "security",
    "middleware"
  ],
  "author": "Sagar Gupta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sagargupta16/mcp-toolkit.git",
    "directory": "packages/cors"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": ">=1.0.0"
  }
}
