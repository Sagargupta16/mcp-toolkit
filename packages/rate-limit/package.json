{
  "name": "@mcp-toolkit/rate-limit",
  "version": "0.1.0",
  "description": "Rate limiting middleware for MCP servers - token bucket strategy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "rate-limit",
    "rate-limiting",
    "token-bucket",
    "middleware"
  ],
  "author": "Sagar Gupta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/prsagar16/mcp-toolkit.git",
    "directory": "packages/rate-limit"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": ">=1.0.0"
  }
}
